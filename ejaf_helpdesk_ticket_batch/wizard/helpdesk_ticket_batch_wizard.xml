<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="helpdesck_ticket_wizard" model="ir.ui.view">
            <field name="name">helpdesck ticket wizard</field>
            <field name="model">helpdesck.ticket.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="batch_id" invisible="1"/>
                            <field name="batch_type" invisible="1"/>
                        </group>
                        <notebook>
                            <page string="Tickets Customer" attrs="{'invisible': [('batch_type', '!=', 'customers')]}">
                                <field name="partner_ids" domain="[('customer_rank' ,'>', 0)]"/>
                            </page>

                            <page string="Tickets Serials" attrs="{'invisible': [('batch_type', '!=', 'serials')]}">
                                <field name="serial_ids" widget="many2many_tags"/>
                            </page>
                        </notebook>


                    </sheet>

                    <footer>
                        <button name="compute_sheet" string="Start Tickets" type="object"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_helpdesk_ticket_wizard" model="ir.actions.act_window">
            <field name="name">Generate Ticets</field>
            <field name="res_model">helpdesck.ticket.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>