<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_procurement_compute_by_category_wizard" model="ir.ui.view">
        <field name="name">Run Schedulers Manually By Category</field>
        <field name="model">stock.scheduler.compute</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <form string="Parameters">
                <group>
                    <field name="categ_ids" widget="many2many_tags" required="1"/>
                </group>
                <p>
                The stock will be reserved for operations waiting for availability and the reordering rules will be triggered for the selected product categories.
                </p>
                <footer>
                    <button name="procure_calculation_by_categ" string="Run Scheduler" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_procurement_compute_by_category" model="ir.actions.act_window">
        <field name="name">Run Scheduler By Category</field>
        <field name="res_model">stock.scheduler.compute</field>
        <field name="view_mode">form</field>
        <field name="view_ids" eval="[(5, 0, 0), (0, 0, {'view_mode': 'form', 'view_id': ref('ejaf_scheduler_by_category.view_procurement_compute_by_category_wizard')})]"/>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_procurement_compute_by_category" action="action_procurement_compute_by_category" parent="stock.menu_stock_warehouse_mgmt" sequence="136"/>


</odoo>
